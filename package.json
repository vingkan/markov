{
  "name": "markov",
  "description": "Fun with Markov Chains!",
  "version": "0.0.0",
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "dependencies": {
    "jquery": "^2.1.4"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "watchify": "^3.6.1"
  },
  "main": "bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify client/scripts/main.js -o client/bundle.js",
    "watch": "node_modules/.bin/watchify client/scripts/main.js -o client/bundle.js --debug --verbose",
    "launch": "node server.js",
    "dev": "start cmd.exe & start cmd.exe /k npm run launch & npm run watch"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/vingkan/omnipointment.git"
  },
  "author": "Vinesh Kannan",
  "license": "ISC"
}
